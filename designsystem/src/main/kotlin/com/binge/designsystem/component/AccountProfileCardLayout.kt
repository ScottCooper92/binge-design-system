package com.binge.designsystem.component

/** How [AccountProfileCard] arranges its avatar and text. */
enum class AccountProfileCardLayout {
    /** Avatar left, text column right — the wide/short phone header. */
    Row,

    /** Square surface with avatar centred on top and text stacked below — the expanded left pane. */
    Column,
}
